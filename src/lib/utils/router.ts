// export const redirect = (endpoint: string, delay = 0) => {
// 	setTimeout(() => {
// 		window.location.href = endpoint;
// 	}, delay);
// };

// export const redirectNotAuth = (endpoint: string, delay = 0) => {
// 	redirect(`/0_0/redirect/no-auth?url=${endpoint}`, delay);
// }